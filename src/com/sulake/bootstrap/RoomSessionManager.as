package com.sulake.bootstrap
{
    import com.sulake.habbo.session.RoomSessionManager;
    import com.sulake.core.runtime.IContext;

    public class RoomSessionManager extends com.sulake.habbo.session.RoomSessionManager 
    {

        public function RoomSessionManager(param1:IContext, param2:uint=0)
        {
            super(param1, param2);
        }

    }
}